{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "watchPatterns": [
            "server/**"
        ]
    },
    "deploy": {
        "startCommand": "npm start",
        "serviceName": "daily-expense-server",
        "rootDirectory": "server"
    }
}